<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.vuebackend.mapper.SampleMapper">

  <select id="selectNow" resultType="string">
    SELECT NOW() 
  </select>

  <select id="select2" resultType="string">
    SELECT 22 
  </select>

</mapper>
