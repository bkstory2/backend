# DevExtreme 파일 업로드 구현 계획

## 📋 개요
Vue.js + DevExtreme을 사용하여 게시판에 파일 첨부 기능 구현

---

## 🔧 구현 단계

### 1. Frontend 준비 (Vue.js)
- [ ] DevExtreme 라이브러리 설치
  ```
  npm install devextreme devextreme-vue
  ```

- [ ] Write.vue 수정
  - DxFileUploader 컴포넌트 추가
  - 파일 업로드 URL 설정: `/api/board/upload`
  - 파일 선택/제거 이벤트 처리
  - 선택된 파일 표시

- [ ] Update.vue 수정
  - 기존 파일 표시
  - 새 파일 업로드 옵션

### 2. Backend 준비 (Spring Boot)

#### 2-1. 폴더 생성
- [ ] 서버 업로드 폴더: `backend/uploads/board/`

#### 2-2. Board.java 수정
- [ ] `fileNm` 필드 추가
  ```java
  private String fileNm;
  ```

#### 2-3. BoardController.java 추가
- [ ] 파일 업로드 엔드포인트
  ```java
  @PostMapping("/upload")
  public ResponseEntity<String> uploadFile(@RequestParam("file") MultipartFile file)
  ```
  - 파일 검증 (크기, 확장자)
  - 서버에 저장
  - 파일명 반환

#### 2-4. BoardService.java 수정
- [ ] 파일 저장 로직
  - UUID로 파일명 변경
  - 저장 경로 설정

#### 2-5. application.yml 수정
- [ ] 파일 업로드 설정
  ```yaml
  spring:
    servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 10MB
  ```

#### 2-6. BoardMapper.xml 수정
- [ ] INSERT, UPDATE 쿼리에 fileNm 포함

### 3. Database 수정
- [ ] board 테이블에 file_nm 컬럼 유지

### 4. API 명세

#### 파일 업로드
```
POST /api/board/upload
Content-Type: multipart/form-data

Request:
- file: File

Response:
{
  "fileName": "uuid-filename.pdf"
}
```

#### 게시글 저장 (기존)
```
POST /api/board
Content-Type: application/json

Request:
{
  "board_id": "free",
  "user_id": "john",
  "title": "제목",
  "body": "내용",
  "file_nm": "uuid-filename.pdf"  ← 새로 추가
}
```

---

## 📁 파일 구조

```
backend/
├── src/main/java/
│   └── com/example/vuebackend/
│       ├── controller/BoardController.java (수정)
│       ├── service/BoardService.java (수정)
│       └── model/Board.java (수정)
├── uploads/board/          ← 새 폴더
│   ├── uuid-1.pdf
│   ├── uuid-2.jpg
│   └── ...
└── src/main/resources/
    ├── application.yml (수정)
    └── mapper/board/BoardMapper.xml (수정)

frontend/
└── src/views/
    ├── Write.vue (수정) - DxFileUploader 추가
    └── Update.vue (수정) - 파일 관리
```

---

## ⚠️ 고려사항

1. **파일 검증**
   - 허용 확장자: pdf, jpg, jpeg, png, doc, docx
   - 최대 파일 크기: 10MB

2. **보안**
   - 파일명 UUID로 변경 (원본명 숨김)
   - 악성 파일 업로드 방지
   - CORS 설정

3. **파일 삭제**
   - 게시글 삭제 시 파일도 삭제
   - 파일 업데이트 시 이전 파일 삭제

4. **다운로드**
   - GET /api/board/download?file_nm=uuid-1.pdf
   - 나중에 구현 예정

---

## 🚀 진행 순서

1. application.yml 수정
2. Board.java 수정
3. BoardController 수정
4. BoardService 수정
5. BoardMapper.xml 수정
6. 폴더 생성 (uploads/board/)
7. Frontend DevExtreme 설치
8. Write.vue 수정
9. Update.vue 수정
10. 테스트
